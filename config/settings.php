<?php
/**
 * Local settings file for quick configuration during development.
 *
 * Copy this file to configure your local database credentials, or set
 * the corresponding environment variables (DB_HOST, DB_USER, DB_PASS, DB_NAME).
 *
 * IMPORTANT: Do NOT commit real credentials to source control in production.
 */

// Example local DB credentials for XAMPP default setup (adjust as needed):
$DB_HOST = 'localhost';
$DB_USER = 'root';
$DB_PASS = '';
$DB_NAME = 'number_plate_db';

// You can override by setting the variables above. Leave blank to use environment/defaults.
?>